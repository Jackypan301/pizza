import React from 'react'
import ListItems from './ListItems.jsx'
var List = (props) => (
  <ul>
    {/* {props.toppings.map((topping, index) => (<ListItem  topping={topping} key={index} />))} */}
    apple
  </ul>
)

export default List